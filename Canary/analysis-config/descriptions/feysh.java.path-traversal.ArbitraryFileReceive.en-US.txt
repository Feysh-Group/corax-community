When the server implements the file upload function, if the file path of the uploaded file is controllable by the user, and the server directly "receives the file and creates the file" to an untrusted path that can be controlled by the attacker without security filtering, it may create dangerous files or cause arbitrary file overwriting and forgery.